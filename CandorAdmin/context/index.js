import React from "react";

export const ModalData = React.createContext({});
export const NotiData = React.createContext({});
export const AdminData = React.createContext({});
export const PhoneData = React.createContext({});
